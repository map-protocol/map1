# Changelog

All notable changes to map1 are documented here.

Implementation versions use semantic versioning.
Spec versioning is defined in /spec/VERSIONING.md.

---

## [1.0.0] - 2026-02-23

### Added
- MAP v1.0 spec (frozen)
- Python implementation
- Node/TypeScript implementation
- CLI (installed via Python package; `map1 mid --full`, `map1 mid --bind`, `map1 canon`, `map1 version`)
- 53 conformance vectors
- PASS_REPORT schema and verification tooling
